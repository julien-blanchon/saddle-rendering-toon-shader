# Example Assets

- `models/FlightHelmet/`
  - Source: copied from the local Bevy 0.18 example assets at `shared/context/bevy/assets/models/FlightHelmet/`
  - Purpose: textured multi-mesh glTF scene for the replacement example and lab
  - Contents: `FlightHelmet.gltf`, mesh buffers, and the five referenced PBR texture sets

The rest of the example content is generated procedurally in code so the crate stays self-contained and easy to iterate on.
